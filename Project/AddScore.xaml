<Window x:Class="Project.AddScore"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project"
        mc:Ignorable="d"
        Title="Add Score" Height="550" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Select Class:"/>
            <ComboBox x:Name="cbClass" DisplayMemberPath="ClassName" SelectionChanged="cbClass_SelectionChanged"/>

            <TextBlock Text="Select Student:" Margin="0,10,0,0"/>
            <ComboBox x:Name="cbStudent" DisplayMemberPath="FullName"/>

            <TextBlock Text="Week Number:" Margin="0,10,0,0"/>
            <TextBox x:Name="txtWeek"/>

            <Grid Grid.Row="2" Width="350" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Scores -->
                <TextBlock Text="Scores (0 - 9):" FontSize="15" FontWeight="Bold" Grid.Row="0"/>

                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,5">
                    <StackPanel Margin="10">
                        <TextBlock Text="Listening"/>
                        <TextBox x:Name="txtListening" Width="50"/>
                    </StackPanel>
                    <StackPanel Margin="10">
                        <TextBlock Text="Speaking"/>
                        <TextBox x:Name="txtSpeaking" Width="50"/>
                    </StackPanel>
                    <StackPanel Margin="10">
                        <TextBlock Text="Reading"/>
                        <TextBox x:Name="txtReading" Width="50"/>
                    </StackPanel>
                    <StackPanel Margin="10">
                        <TextBlock Text="Writing"/>
                        <TextBox x:Name="txtWriting" Width="50"/>
                    </StackPanel>
                </StackPanel>

                <!-- AI Suggestions -->
                <TextBlock Text="AI Suggestion:"  Grid.Row="2" Margin="0,10,0,5"/>
                <TextBox x:Name="txtAISuggestion" Grid.Row="3" Height="80" TextWrapping="Wrap"
         IsReadOnly="True" VerticalScrollBarVisibility="Auto" />

                <!-- Weekly Feedback -->
                <TextBlock Text="Teacher Feedback:" Grid.Row="4" Margin="0,10,0,5"/>
                <TextBox x:Name="txtFeedback" Grid.Row="5" AcceptsReturn="True" Height="80"
         TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0" />
            </Grid>

            <TextBlock x:Name="txtStatus" Foreground="Red" Margin="0,5"/>

            <Button Content="Save" Click="Save_Click" Margin="0,10,0,0" Width="50"/>
        </StackPanel>
    </Grid>
</Window>

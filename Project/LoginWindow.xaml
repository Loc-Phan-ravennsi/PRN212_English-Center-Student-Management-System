<Window x:Class="Project.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="Đăng nhập" Height="250" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Đăng Nhập" FontSize="18" FontWeight="Bold" Grid.Row="0" Grid.Column="0" Margin="0,0,20,0" VerticalAlignment="Center"/>

        <!-- Email -->
        <TextBlock Text="Email:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="2" Grid.Column="1" Margin="0,5" VerticalAlignment="Center"/>

        <!-- Password -->
        <TextBlock Text="Mật khẩu:" Grid.Row="3" Grid.Column="0" Margin="0,10,20,0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="0,10,0,0" VerticalAlignment="Center"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Đăng nhập" Background="LightGreen" Width="70" Height="28" Margin="0,0,10,0" Click="Login_Click"/>
            <Button Content="Thoát" Width="60" Height="28" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
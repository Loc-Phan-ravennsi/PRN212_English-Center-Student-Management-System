<Window x:Class="Project.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="Đăng nhập" Height="250" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Log In" FontSize="18" FontWeight="Bold" Grid.Row="0" Grid.Column="0" Margin="0,0,20,0" VerticalAlignment="Center"/>
        <TextBlock Name="tbError" Grid.Row="0" Grid.Column="1" Foreground="Red"></TextBlock>
        <!-- Email -->
        <TextBlock Text="Email:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="tbEmail" Grid.Row="2" Grid.Column="1" Margin="0,5" VerticalAlignment="Center"/>

        <!-- Password -->
        <TextBlock Text="Password:" Grid.Row="3" Grid.Column="0" Margin="0,10,20,0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="pbPassword" Grid.Row="3" Grid.Column="1" Margin="0,10,0,0" VerticalAlignment="Center"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Name="btnLogin" Content="Log in" Background="LightGreen" Width="70" Height="28" Margin="0,0,10,0" Click="btnLogin_Click"/>
            <Button Name="btnExit" Content="Exit" Background="DarkRed" Foreground="White" Width="70" Height="28" Click="btnExit_Click"/>
        </StackPanel>
    </Grid>
</Window>